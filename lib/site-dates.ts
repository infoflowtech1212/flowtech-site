// Real last-content-change date (update when the site's actual content
// changes — not on every build/request, which is what made this signal
// meaningless before). Shared by the sitemap and structured data so they
// never drift apart.
export const CONTENT_LAST_MODIFIED = "2026-07-13";
